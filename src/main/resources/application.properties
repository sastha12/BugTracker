spring.application.name=Bugtracker

# =========================================================
# 1. DATABASE CONFIGURATION (FIXED FOR RENDER DEPLOYMENT)
# The correct way to use the full URL provided by Render is just ${DB_URL}.
# =========================================================
spring.datasource.url=jdbc:postgresql://bugtrackerbd_user:5tTePy5pz7RldkukNeQqKfe1esbBAveN@dpg-d4dnadjuibrs73dp13ag-a.oregon-postgres.render.com/bugtrackerbd
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# Specify the PostgreSQL Dialect directly as per your Render database type.
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# =========================================================
# 2. SECURITY FIX (EMAIL CREDENTIALS)
# The values are now sourced securely from Environment Variables.
# =========================================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=sasthasastha4@gmail.com
spring.mail.password=bits xnxg ytvw ifsm
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# =========================================================
# 3. OTHER CONFIGURATIONS
# =========================================================
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp
server.port=8080
